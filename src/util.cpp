#include <util.h>

void a3de::shellsort (int *array, int elemente)
{
    int i, j, temp, n;

    /* Anfangswert fü^ elemente; i++) {
            temp = array[i];
            /* Größer als temp und nicht elemente
            * sowie >= und nicht > */
            for(j = i;
                j >= n && array[j-n] > temp;
                j -= n) {
            array[j] = array[j-n];
            }
            array[j] = temp;
        //}
    //}
}